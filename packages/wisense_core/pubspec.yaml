name: wisense_core
description: Minimal WiSense core stubs for Apex Scheduler standalone builds.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
