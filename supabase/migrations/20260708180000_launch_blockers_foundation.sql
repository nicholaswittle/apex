-- Launch blockers foundation migration placeholder.
-- Full schema changes documented in supabase/migrations/README.md
-- (shift_date / task_date columns, swap claim columns, day sync triggers).

SELECT 1;
